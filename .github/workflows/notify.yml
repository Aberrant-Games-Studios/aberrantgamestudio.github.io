name: Notify Discord on News Update

on:
  push:
    branches: [main]
    paths:
      - 'news.json'

jobs:
  discord-notify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read latest post and fire Discord webhook
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          python3 - <<'PYEOF'
          import json, subprocess, os, urllib.request

          with open('news.json') as f:
            posts = json.load(f)

          posts.sort(key=lambda p: p['date'], reverse=True)
          p = posts[0]

          tag   = p.get('tag', 'Update')
          date  = p['date']
          title = p['title']
          blurb = p['blurb']
          pid   = p['id']
          image = p.get('image', '')

          post_url = f"https://www.aberrantgamestudio.com/news.html#{pid}"

          color = 7829503
          if 'Announce' in tag: color = 14680064
          if 'Dev Log'  in tag: color = 2829617

          embed = {
            'color': color,
            'author': {'name': f'{tag}  ·  {date}'},
            'title': title,
            'description': blurb,
            'url': post_url,
            'footer': {'text': 'aberrantgamestudio.com'}
          }

          if image:
            embed['image'] = {'url': f'https://www.aberrantgamestudio.com/{image}'}

          payload = json.dumps({'username': 'ORDER // ONE', 'embeds': [embed]}).encode()

          webhook = os.environ['DISCORD_WEBHOOK']
          
          # Added User-Agent to bypass Discord/Cloudflare blocking
          headers = {
              'Content-Type': 'application/json',
              'User-Agent': 'AberrantGamesWebhook/1.0'
          }
          
          req = urllib.request.Request(webhook, data=payload, headers=headers)
          with urllib.request.urlopen(req) as res:
            print(f"Discord responded: {res.status}")
          PYEOF